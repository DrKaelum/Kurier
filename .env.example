# Development-only PostgreSQL settings used by compose.yml.
# Do not reuse these credentials outside local development.
POSTGRES_DB=kurier
POSTGRES_USER=kurier_local
POSTGRES_PASSWORD=kurier_local_only
POSTGRES_PORT=5432
